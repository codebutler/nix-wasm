# oxfmt reads this file as its ignore list (--ignore-path defaults to .gitignore and .prettierignore).
# Vendored third-party bundles — never format-checked.
# (Path is relative to runtime/, where oxfmt runs — must be demo/web/vendor/, not web/vendor/.)
demo/web/vendor/
