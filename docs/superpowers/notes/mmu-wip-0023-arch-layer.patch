# WIP (#128 Track A kernel half) — CONFIG_MMU=y arch/wasm layer.
# STATUS: compiles + links end-to-end (vmlinux built); NOMMU build unregressed.
# Includes the semantic pieces: switch_mm/activate_mm -> env.__mmu_set_pt_base
# (verified present in the vmlinux import section), and the software uaccess
# table-walk (raw_copy_{to,from}_user/__clear_user walk the user page table;
# UACCESS_MEMCPY is now NOMMU-only). NOT YET BOOTING and NOT wired into
# kernel.nix — still to come: exec/binfmt_wasm address-space setup, the
# __mmu_fault -> do_page_fault entry (A2), the pass 2-level walk, and the
# engine's set_pt_base + instrumented-exec integration. Apply AFTER 0004-0022.
--- a/arch/wasm/include/asm/pgtable.h
+++ b/arch/wasm/include/asm/pgtable.h
@@ -3,17 +3,16 @@
 #ifndef _ASM_WASM_PGTABLE_H
 #define _ASM_WASM_PGTABLE_H
 
-#include <asm-generic/pgtable-nopmd.h>
-
 #include <linux/slab.h>
 #include <asm/processor.h>
 #include <asm/page.h>
 #include <asm/io.h>
 
-/*
- * No MMU support so do nothing...
- * Inspired by the various other NOMMU implementations in the kernel.
- */
+#ifndef CONFIG_MMU
+/* ----------------------------------------------------------------------------
+ * NOMMU: no MMU support, everything is a no-op (the historical wasm path).
+ * -------------------------------------------------------------------------- */
+#include <asm-generic/pgtable-nopmd.h>
 
 #define pgd_present(pgd)	(1)
 #define pgd_none(pgd)		(0)
@@ -34,11 +33,125 @@
 extern void paging_init(void);
 #define swapper_pg_dir ((pgd_t *) 0)
 
-/*
- * ZERO_PAGE is a global shared page that is always zero: used
- * for zero-mapped memory areas etc..
- */
 extern unsigned long empty_zero_page[PAGE_SIZE / sizeof(unsigned long)];
 #define ZERO_PAGE(vaddr) (virt_to_page(empty_zero_page))
 
+#else /* CONFIG_MMU */
+/* ----------------------------------------------------------------------------
+ * Software MMU (Track A, #128). Classic 32-bit 2-level split matching the
+ * instrumentation pass's translate (docs/.../softmmu-kernel-design.md §1/§2):
+ *   pgd_e = u32[ pt_base + (va>>22)<<2 ]                   level 1 (PTE-table base)
+ *   pte   = u32[ (pgd_e & ~0xfff) + ((va>>12 & 0x3ff)<<2) ] level 2
+ *   phys  = (pte & ~0xfff) + (va & 0xfff)
+ * PTE: bits 0-11 flags, bits 12-31 physical page base. The kernel runs in
+ * physical (identity) space; only instrumented USER code translates.
+ * -------------------------------------------------------------------------- */
+#include <asm-generic/pgtable-nopmd.h>
+
+/* 2-level: PGD (1024 entries, each covers 4MiB) -> PTE (1024 entries/page). */
+#define PGDIR_SHIFT	22
+#define PTRS_PER_PGD	1024
+#define PTRS_PER_PTE	1024
+#define PGDIR_SIZE	(1UL << PGDIR_SHIFT)
+#define PGDIR_MASK	(~(PGDIR_SIZE - 1))
+#define PFN_PTE_SHIFT	PAGE_SHIFT
+#define USER_PTRS_PER_PGD	(TASK_SIZE >> PGDIR_SHIFT)
+
+/* PTE flag bits (low 12, ignored by the A1 fast-path translate). */
+#define _PAGE_PRESENT	(1UL << 0)
+#define _PAGE_WRITE	(1UL << 1)
+#define _PAGE_USER	(1UL << 2)
+#define _PAGE_ACCESSED	(1UL << 3)
+#define _PAGE_DIRTY	(1UL << 4)
+#define _PAGE_CHG_MASK	(PAGE_MASK | _PAGE_ACCESSED | _PAGE_DIRTY)
+
+#define _PAGE_TABLE	(_PAGE_PRESENT | _PAGE_WRITE | _PAGE_USER | _PAGE_ACCESSED)
+
+#define PAGE_NONE	__pgprot(_PAGE_PRESENT | _PAGE_ACCESSED)
+#define PAGE_SHARED	__pgprot(_PAGE_PRESENT | _PAGE_WRITE | _PAGE_USER | _PAGE_ACCESSED)
+#define PAGE_COPY	__pgprot(_PAGE_PRESENT | _PAGE_USER | _PAGE_ACCESSED)
+#define PAGE_READONLY	__pgprot(_PAGE_PRESENT | _PAGE_USER | _PAGE_ACCESSED)
+#define PAGE_KERNEL	__pgprot(_PAGE_PRESENT | _PAGE_WRITE | _PAGE_ACCESSED)
+
+#define VMALLOC_START	0xC0000000UL
+#define VMALLOC_END	0xFF000000UL
+#define KMAP_START	0
+#define KMAP_END	0xFFFFFFFF
+
+extern pgd_t swapper_pg_dir[PTRS_PER_PGD];
+extern void paging_init(void);
+
+extern unsigned long empty_zero_page[PAGE_SIZE / sizeof(unsigned long)];
+#define ZERO_PAGE(vaddr) (virt_to_page(empty_zero_page))
+
+/* ---- PTE queries / constructors ---- */
+static inline int pte_none(pte_t pte)		{ return !pte_val(pte); }
+static inline int pte_present(pte_t pte)	{ return pte_val(pte) & _PAGE_PRESENT; }
+static inline int pte_write(pte_t pte)		{ return pte_val(pte) & _PAGE_WRITE; }
+static inline int pte_dirty(pte_t pte)		{ return pte_val(pte) & _PAGE_DIRTY; }
+static inline int pte_young(pte_t pte)		{ return pte_val(pte) & _PAGE_ACCESSED; }
+
+static inline pte_t pte_wrprotect(pte_t pte)	{ pte_val(pte) &= ~_PAGE_WRITE; return pte; }
+static inline pte_t pte_mkclean(pte_t pte)	{ pte_val(pte) &= ~_PAGE_DIRTY; return pte; }
+static inline pte_t pte_mkold(pte_t pte)	{ pte_val(pte) &= ~_PAGE_ACCESSED; return pte; }
+static inline pte_t pte_mkwrite_novma(pte_t pte){ pte_val(pte) |= _PAGE_WRITE; return pte; }
+static inline pte_t pte_mkdirty(pte_t pte)	{ pte_val(pte) |= _PAGE_DIRTY; return pte; }
+static inline pte_t pte_mkyoung(pte_t pte)	{ pte_val(pte) |= _PAGE_ACCESSED; return pte; }
+
+#define pte_pfn(pte)		(pte_val(pte) >> PAGE_SHIFT)
+#define pfn_pte(pfn, prot)	__pte(((pfn) << PAGE_SHIFT) | pgprot_val(prot))
+#define pte_page(pte)		pfn_to_page(pte_pfn(pte))
+
+static inline pte_t pte_modify(pte_t pte, pgprot_t newprot)
+{
+	pte_val(pte) = (pte_val(pte) & _PAGE_CHG_MASK) | pgprot_val(newprot);
+	return pte;
+}
+
+#define set_pte(ptep, pte)	(*(ptep) = (pte))
+#define pte_clear(mm, addr, ptep) set_pte((ptep), __pte(0))
+
+/* ---- PMD (folded into PGD by nopmd) ---- */
+static inline int pmd_none(pmd_t pmd)		{ return !pmd_val(pmd); }
+static inline int pmd_bad(pmd_t pmd)		{ return 0; }
+static inline int pmd_present(pmd_t pmd)	{ return pmd_val(pmd); }
+static inline void pmd_clear(pmd_t *pmdp)	{ pmd_val(*pmdp) = 0; }
+static inline unsigned long pmd_page_vaddr(pmd_t pmd)
+{
+	return (unsigned long)__va(pmd_val(pmd) & PAGE_MASK);
+}
+#define pmd_pfn(pmd)		(pmd_val(pmd) >> PAGE_SHIFT)
+#define set_pmd(pmdp, pmd)	(*(pmdp) = (pmd))
+#define pmd_page(pmd)		pfn_to_page(pmd_pfn(pmd))
+
+/* pte table pointer from a (folded) pmd entry */
+#define pte_offset_kernel(pmd, addr) \
+	((pte_t *)pmd_page_vaddr(*(pmd)) + pte_index(addr))
+
+
+
+/* ---- swap entry encoding (present=0 PTEs). type:bits3-7, offset:bits8-31 ---- */
+#define _PAGE_SWP_EXCLUSIVE	_PAGE_USER
+#define __swp_type(x)		(((x).val >> 3) & 0x1f)
+#define __swp_offset(x)		((x).val >> 8)
+#define __swp_entry(type, offset) \
+	((swp_entry_t){ ((type) << 3) | ((offset) << 8) })
+#define __pte_to_swp_entry(pte)	((swp_entry_t){ pte_val(pte) })
+#define __swp_entry_to_pte(x)	((pte_t){ (x).val })
+static inline int pte_swp_exclusive(pte_t pte)
+{ return pte_val(pte) & _PAGE_SWP_EXCLUSIVE; }
+static inline pte_t pte_swp_mkexclusive(pte_t pte)
+{ pte_val(pte) |= _PAGE_SWP_EXCLUSIVE; return pte; }
+static inline pte_t pte_swp_clear_exclusive(pte_t pte)
+{ pte_val(pte) &= ~_PAGE_SWP_EXCLUSIVE; return pte; }
+
+#define pgd_ERROR(e) \
+	pr_err("%s:%d: bad pgd %08lx.\n", __FILE__, __LINE__, pgd_val(e))
+
+
+#define update_mmu_cache_range(vmf, vma, addr, ptep, nr) do { } while (0)
+#define update_mmu_cache(vma, addr, ptep) do { } while (0)
+#endif /* CONFIG_MMU */
+
+#include <asm-generic/getorder.h>
 #endif /* _ASM_WASM_PGTABLE_H */
--- a/arch/wasm/include/asm/processor.h
+++ b/arch/wasm/include/asm/processor.h
@@ -12,6 +12,11 @@
 
 /* 3 GB RAM for userspace, 1 GB for the kernel. */
 #define TASK_SIZE (0xC0000000)
+#ifdef CONFIG_MMU
+#define STACK_TOP	TASK_SIZE
+#define STACK_TOP_MAX	STACK_TOP
+#define TASK_UNMAPPED_BASE	(PAGE_ALIGN(TASK_SIZE / 3))
+#endif
 
 /*
  * We run interrupts on CPU 1, keep it clear. Why not CPU 0? Because init needs
--- a/arch/wasm/include/asm/io.h
+++ b/arch/wasm/include/asm/io.h
@@ -6,6 +6,11 @@
 /* asm-generic/io.h defines this as NULL which leads to compiler warnings. */
 #define PCI_IOBASE ((void __iomem *)sizeof(long))
 
+#ifdef CONFIG_MMU
+#define ioremap(addr, size)	((void __iomem *)(unsigned long)(addr))
+#define iounmap(addr)		do { } while (0)
+#endif
+
 #include <asm-generic/io.h>
 
 #endif /* _ASM_WASM_IO_H */
--- a/arch/wasm/include/asm/elf.h
+++ b/arch/wasm/include/asm/elf.h
@@ -3,6 +3,15 @@
 #ifndef _ASM_WASM_ELF_H
 #define _ASM_WASM_ELF_H
 
+#define elf_check_arch(x) (1)
+#define ELF_HWCAP	(0)
+#define ELF_PLATFORM	(NULL)
+#define ELF_EXEC_PAGESIZE	PAGE_SIZE
+
+#ifdef CONFIG_MMU
+#define ELF_ET_DYN_BASE	0x08000000UL
+#endif
+
 #include <uapi/asm/elf.h>
 
 /*
--- a/arch/wasm/include/asm/mmu_context.h
+++ b/arch/wasm/include/asm/mmu_context.h
@@ -1,8 +1,39 @@
 /* SPDX-License-Identifier: GPL-2.0-only */
-
 #ifndef _ASM_WASM_MMU_CONTEXT_H
 #define _ASM_WASM_MMU_CONTEXT_H
 
+#ifdef CONFIG_MMU
+#include <linux/mm_types.h>
+#include <linux/sched.h>
+#include <asm-generic/mm_hooks.h>
+
+/*
+ * The ONE hot ABI action of the software MMU (#128 design §4): on context
+ * switch, hand the incoming process's page-table root to the engine, which
+ * writes it into the instrumented user module's __mmu_pt_base global (the
+ * per-access translate reads it). Host import provided by kernel-worker.js;
+ * the kernel links with --import-undefined so this materializes as an env
+ * import. No TLB to flush — the pass re-reads PTEs on every access.
+ */
+void __mmu_set_pt_base(unsigned long pt_base);
+
+#define switch_mm switch_mm
+static inline void switch_mm(struct mm_struct *prev, struct mm_struct *next,
+			     struct task_struct *tsk)
+{
+	if (prev != next)
+		__mmu_set_pt_base((unsigned long)next->pgd);
+}
+
+#define activate_mm activate_mm
+static inline void activate_mm(struct mm_struct *prev, struct mm_struct *next)
+{
+	__mmu_set_pt_base((unsigned long)next->pgd);
+}
+
+#include <asm-generic/mmu_context.h>
+#else
 #include <asm-generic/nommu_context.h>
+#endif
 
 #endif /* _ASM_WASM_MMU_CONTEXT_H */
--- a/arch/wasm/mm/Makefile
+++ b/arch/wasm/mm/Makefile
@@ -1,3 +1,4 @@
 # SPDX-License-Identifier: GPL-2.0-only
 
 obj-y += init.o
+obj-$(CONFIG_MMU) += pgtable.o uaccess.o
--- /dev/null
+++ b/arch/wasm/include/asm/tlbflush.h
@@ -0,0 +1,18 @@
+/* SPDX-License-Identifier: GPL-2.0-only */
+#ifndef _ASM_WASM_TLBFLUSH_H
+#define _ASM_WASM_TLBFLUSH_H
+#ifdef CONFIG_MMU
+#include <linux/mm_types.h>
+/* No hardware TLB: the instrumentation pass re-reads the PTE on every access,
+ * so a mapping change is visible immediately. All flushes are no-ops (#128). */
+static inline void flush_tlb_all(void) {}
+static inline void flush_tlb_mm(struct mm_struct *mm) {}
+static inline void flush_tlb_page(struct vm_area_struct *vma, unsigned long a) {}
+static inline void flush_tlb_range(struct vm_area_struct *vma,
+				   unsigned long s, unsigned long e) {}
+static inline void flush_tlb_kernel_range(unsigned long s, unsigned long e) {}
+static inline void local_flush_tlb_all(void) {}
+#else
+#include <asm-generic/tlbflush.h>
+#endif
+#endif
--- /dev/null
+++ b/arch/wasm/include/asm/pgalloc.h
@@ -0,0 +1,14 @@
+/* SPDX-License-Identifier: GPL-2.0-only */
+#ifndef _ASM_WASM_PGALLOC_H
+#define _ASM_WASM_PGALLOC_H
+#ifdef CONFIG_MMU
+#include <linux/mm.h>
+#include <asm-generic/pgalloc.h>
+static inline void pmd_populate_kernel(struct mm_struct *mm, pmd_t *pmd, pte_t *pte)
+{ set_pmd(pmd, __pmd((unsigned long)pte)); }
+static inline void pmd_populate(struct mm_struct *mm, pmd_t *pmd, pgtable_t pte)
+{ set_pmd(pmd, __pmd((unsigned long)page_address(pte))); }
+extern pgd_t *pgd_alloc(struct mm_struct *mm);
+#define __pte_free_tlb(tlb, pte, addr) tlb_remove_ptdesc((tlb), page_ptdesc(pte))
+#endif /* CONFIG_MMU */
+#endif
--- /dev/null
+++ b/arch/wasm/include/asm/uaccess.h
@@ -0,0 +1,43 @@
+/* SPDX-License-Identifier: GPL-2.0-only */
+#ifndef _ASM_WASM_UACCESS_H
+#define _ASM_WASM_UACCESS_H
+
+#ifdef CONFIG_MMU
+/*
+ * Software-MMU uaccess (#128). The KERNEL runs uninstrumented in physical/
+ * identity space; USER pointers are virtual (translated by the instrumented
+ * user code via the per-process page table). So kernel<->user copies must WALK
+ * the user page table in software (arch/wasm/mm/uaccess.c) — the equivalent of
+ * a hardware arch taking a fault on a bad user pointer, here a soft lookup.
+ * Kernel-nofault access stays plain (kernel addresses are identity).
+ */
+#include <linux/string.h>
+#include <linux/unaligned.h>
+#include <asm-generic/access_ok.h>
+
+unsigned long raw_copy_from_user(void *to, const void __user *from, unsigned long n);
+unsigned long raw_copy_to_user(void __user *to, const void *from, unsigned long n);
+unsigned long __clear_user(void __user *to, unsigned long n);
+#define __clear_user __clear_user
+
+#define __get_kernel_nofault(dst, src, type, err_label)			\
+do {									\
+	*((type *)dst) = get_unaligned((type *)(src));			\
+	if (0) /* make sure the label looks used to the compiler */	\
+		goto err_label;						\
+} while (0)
+
+#define __put_kernel_nofault(dst, src, type, err_label)			\
+do {									\
+	put_unaligned(*((type *)src), (type *)(dst));			\
+	if (0) /* make sure the label looks used to the compiler */	\
+		goto err_label;						\
+} while (0)
+
+#include <asm-generic/uaccess.h>
+
+#else /* !CONFIG_MMU — the historical flat path (UACCESS_MEMCPY) */
+#include <asm-generic/uaccess.h>
+#endif
+
+#endif /* _ASM_WASM_UACCESS_H */
--- /dev/null
+++ b/arch/wasm/mm/pgtable.c
@@ -0,0 +1,32 @@
+// SPDX-License-Identifier: GPL-2.0-only
+/* Software-MMU page-table bringup (Track A, #128). */
+#include <linux/mm.h>
+#include <linux/memblock.h>
+#include <linux/gfp.h>
+#include <linux/string.h>
+#include <asm/pgtable.h>
+#include <asm/pgalloc.h>
+
+/* The master kernel page directory. */
+pgd_t swapper_pg_dir[PTRS_PER_PGD] __page_aligned_bss;
+EXPORT_SYMBOL(swapper_pg_dir);
+
+pgd_t *pgd_alloc(struct mm_struct *mm)
+{
+	pgd_t *pgd = (pgd_t *)__get_free_page(GFP_KERNEL);
+
+	if (!pgd)
+		return NULL;
+	/* User half zeroed; kernel half copied from the master table. */
+	memset(pgd, 0, USER_PTRS_PER_PGD * sizeof(pgd_t));
+	memcpy(pgd + USER_PTRS_PER_PGD, swapper_pg_dir + USER_PTRS_PER_PGD,
+	       (PTRS_PER_PGD - USER_PTRS_PER_PGD) * sizeof(pgd_t));
+	return pgd;
+}
+
+/* Zones are initialised by the generic free_area_init() -> arch_zone_limits_init()
+ * (arch/wasm/mm/init.c). The kernel runs in physical/identity space (uninstrumented),
+ * so swapper_pg_dir needs no kernel mappings; paging_init stays a no-op. */
+void __init paging_init(void)
+{
+}
--- /dev/null
+++ b/arch/wasm/mm/uaccess.c
@@ -0,0 +1,103 @@
+// SPDX-License-Identifier: GPL-2.0-only
+/*
+ * Software-MMU uaccess (#128): kernel<->user copies walk the user page table.
+ * The kernel is identity-mapped (phys); a user va resolves through the
+ * current mm's 2-level table (asm/pgtable.h). An unmapped/absent page is a
+ * fault -> return the remaining byte count, exactly the raw_copy contract.
+ * (A2 will route the miss through handle_mm_fault for demand paging/COW;
+ * A1 exec populates the table up front, so a miss here is a real EFAULT.)
+ */
+#include <linux/mm.h>
+#include <linux/sched.h>
+#include <linux/string.h>
+#include <linux/export.h>
+#include <asm/pgtable.h>
+#include <linux/uaccess.h>
+
+/* Resolve one user page; returns false if unmapped/not-present (or, for
+ * writes, write-protected). *phys gets the physical byte address of va. */
+static bool user_page_phys(unsigned long va, bool write, unsigned long *phys)
+{
+	struct mm_struct *mm = current->mm;
+	pgd_t *pgd;
+	p4d_t *p4d;
+	pud_t *pud;
+	pmd_t *pmd;
+	pte_t *pte;
+
+	if (!mm || va >= TASK_SIZE)
+		return false;
+	pgd = pgd_offset(mm, va);
+	if (pgd_none(*pgd) || pgd_bad(*pgd))
+		return false;
+	p4d = p4d_offset(pgd, va);
+	if (p4d_none(*p4d))
+		return false;
+	pud = pud_offset(p4d, va);
+	if (pud_none(*pud))
+		return false;
+	pmd = pmd_offset(pud, va);
+	if (pmd_none(*pmd) || pmd_bad(*pmd))
+		return false;
+	pte = pte_offset_kernel(pmd, va);
+	if (!pte_present(*pte))
+		return false;
+	if (write && !pte_write(*pte))
+		return false;
+	*phys = (pte_val(*pte) & PAGE_MASK) + (va & ~PAGE_MASK);
+	return true;
+}
+
+/* Copy n bytes across the user boundary, page by page. dir_to_user picks the
+ * direction; returns the number of bytes NOT copied (0 = full success). */
+static unsigned long walk_copy(void *kaddr, unsigned long uva, unsigned long n,
+			       bool to_user)
+{
+	while (n) {
+		unsigned long phys;
+		unsigned long chunk = min(n, PAGE_SIZE - (uva & ~PAGE_MASK));
+
+		if (!user_page_phys(uva, to_user, &phys))
+			return n;
+		if (to_user)
+			memcpy((void *)phys, kaddr, chunk);
+		else
+			memcpy(kaddr, (void *)phys, chunk);
+		kaddr += chunk;
+		uva += chunk;
+		n -= chunk;
+	}
+	return 0;
+}
+
+unsigned long raw_copy_from_user(void *to, const void __user *from,
+				 unsigned long n)
+{
+	return walk_copy(to, (unsigned long)from, n, false);
+}
+EXPORT_SYMBOL(raw_copy_from_user);
+
+unsigned long raw_copy_to_user(void __user *to, const void *from,
+			       unsigned long n)
+{
+	return walk_copy((void *)from, (unsigned long)to, n, true);
+}
+EXPORT_SYMBOL(raw_copy_to_user);
+
+unsigned long __clear_user(void __user *to, unsigned long n)
+{
+	unsigned long uva = (unsigned long)to;
+
+	while (n) {
+		unsigned long phys;
+		unsigned long chunk = min(n, PAGE_SIZE - (uva & ~PAGE_MASK));
+
+		if (!user_page_phys(uva, true, &phys))
+			return n;
+		memset((void *)phys, 0, chunk);
+		uva += chunk;
+		n -= chunk;
+	}
+	return 0;
+}
+EXPORT_SYMBOL(__clear_user);
--- a/arch/wasm/Kconfig
+++ b/arch/wasm/Kconfig
@@ -20,7 +20,7 @@ config WASM
 
 	select THREAD_INFO_IN_TASK
 	select GENERIC_SMP_IDLE_THREAD
-	select UACCESS_MEMCPY
+	select UACCESS_MEMCPY if !MMU
 	select ARCH_USE_QUEUED_RWLOCKS
 	select GENERIC_CPU_DEVICES
 	select GENERIC_CSUM
@@ -122,6 +122,13 @@ config PGTABLE_LEVELS
 	int
 	default 2
 
+config MMU
+	bool "MMU support (software MMU via instrumentation)"
+	default n
+	help
+	  Enable the software MMU (Track A, #128): per-process page tables with
+	  a per-access translate baked into userspace by the instrumentation pass.
+
 config ARCH_FORCE_MAX_ORDER
 	int "Maximum zone order"
 	range 10 17
